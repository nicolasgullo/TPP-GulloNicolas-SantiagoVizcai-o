<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/estatico/estilos/style.css">
    <link rel="shortcut icon" type="image/x-icon" href="/estatico/img/LOGO.png">
    <title>Bussy - Admin</title>
</head>
<body>
    <main class="index">
        <section class="index-section">
            <button id="btnCliente">Login Cliente</button>
            <div class="index-titulo">
                <h1>Panel de Admin <span id="fuente-especial">Bussy</span></h1>
                <p>Ingrese sus credenciales para continuar:</p>
            </div>
            <div class="index-formulario">
                <form id="loginForm" class="login-form" method="post" action="/admin/login">
                    <div class="campo">
                        <label for="email">Email</label>
                        <input type="email" id="email" name="email" placeholder="tu@email.com" required>
                    </div>
                    <div class="campo">
                        <label for="password">Contraseña</label>
                        <input type="password" id="password" name="password" placeholder="••••••••" minlength="6" required>
                    </div>

                    <div class="login-actions">
                        <button type="button" id="btnAutocompletar" class="btn-autofill">
                            Autocompletar
                        </button>
                        <button type="submit" id="btnLogin">
                            Ingresar
                        </button>
                    </div>
                </form>
            </div>
            <% if (error) { %>
                <p style="color:red; margin-top: 1rem;"><%= error %></p>
            <% } %>
        </section>
    </main>
    <script src="/estatico/js/admin.js"></script>
</body>
</html>